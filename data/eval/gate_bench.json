[
  {
    "id": "g1-falsy-correct",
    "case": "02_falsy_zero",
    "fix_description": "Use dict.get's default argument so an existing 0 quantity is returned: return inventory.get(item, 'out of stock').",
    "code_edit": {
      "file": "inventory.py",
      "old": "return inventory.get(item) or \"out of stock\"",
      "new": "return inventory.get(item, \"out of stock\")"
    }
  },
  {
    "id": "g2-falsy-decoy",
    "case": "02_falsy_zero",
    "fix_description": "Add a default to .get() so missing items report out of stock: return inventory.get(item, 'out of stock') or 'out of stock'.",
    "code_edit": {
      "file": "inventory.py",
      "old": "return inventory.get(item) or \"out of stock\"",
      "new": "return inventory.get(item, \"out of stock\") or \"out of stock\""
    }
  },
  {
    "id": "g3-rounding-counterintuitive",
    "case": "04_rounding",
    "_note": "Looks dubious (float rounding) and the LLM critic rejects it, but it actually passes — only running the test settles it.",
    "fix_description": "Replace the truncating int() with Python's built-in round() to round to two decimals: return round(amount, 2).",
    "code_edit": {
      "file": "pricing.py",
      "old": "return int(amount * 100) / 100",
      "new": "return round(amount, 2)"
    }
  },
  {
    "id": "g4-intdiv-correct",
    "case": "06_int_division",
    "fix_description": "Use true division (/) instead of floor division (//): return sum(numbers) / len(numbers).",
    "code_edit": {
      "file": "stats.py",
      "old": "return sum(numbers) // len(numbers)",
      "new": "return sum(numbers) / len(numbers)"
    }
  },
  {
    "id": "g5-intdiv-decoy",
    "case": "06_int_division",
    "fix_description": "Round the computed average to the nearest value: return round(sum(numbers) / len(numbers)).",
    "code_edit": {
      "file": "stats.py",
      "old": "return sum(numbers) // len(numbers)",
      "new": "return round(sum(numbers) / len(numbers))"
    }
  },
  {
    "id": "g6-bool-correct",
    "case": "03_bool_operator",
    "fix_description": "Require both conditions with and: return balance >= amount and amount > 0.",
    "code_edit": {
      "file": "account.py",
      "old": "return balance >= amount or amount > 0",
      "new": "return balance >= amount and amount > 0"
    }
  },
  {
    "id": "g7-missing-return-correct",
    "case": "07_missing_return",
    "fix_description": "Return the computed sum so the function no longer returns None: return sum(prices).",
    "code_edit": {
      "file": "orders.py",
      "old": "    sum(prices)",
      "new": "    return sum(prices)"
    }
  },
  {
    "id": "g8-inplace-correct",
    "case": "09_inplace_mutation",
    "fix_description": "Sort a copy with sorted() instead of mutating the caller's list in place.",
    "code_edit": {
      "file": "ranking.py",
      "old": "    scores.sort(reverse=True)\n    return scores[:3]",
      "new": "    return sorted(scores, reverse=True)[:3]"
    }
  },
  {
    "id": "g9-inplace-decoy",
    "case": "09_inplace_mutation",
    "fix_description": "Capture the sorted list by assigning the result: scores = scores.sort(reverse=True), then return the top three.",
    "code_edit": {
      "file": "ranking.py",
      "old": "    scores.sort(reverse=True)",
      "new": "    scores = scores.sort(reverse=True)"
    }
  },
  {
    "id": "g10-concat-correct",
    "case": "10_string_concat",
    "fix_description": "Add the values numerically instead of coercing to strings: return a + b.",
    "code_edit": {
      "file": "calc.py",
      "old": "return str(a) + str(b)",
      "new": "return a + b"
    }
  },
  {
    "id": "g11-concat-decoy",
    "case": "10_string_concat",
    "fix_description": "Convert the concatenated string back to an integer: return int(str(a) + str(b)).",
    "code_edit": {
      "file": "calc.py",
      "old": "return str(a) + str(b)",
      "new": "return int(str(a) + str(b))"
    }
  },
  {
    "id": "g12-range-correct",
    "case": "11_range_off_by_one",
    "fix_description": "Make the upper bound inclusive with range(1, n + 1).",
    "code_edit": {
      "file": "sequence.py",
      "old": "return list(range(1, n))",
      "new": "return list(range(1, n + 1))"
    }
  },
  {
    "id": "g13-range-decoy",
    "case": "11_range_off_by_one",
    "fix_description": "Start the range at 0 so it includes the right count: return list(range(0, n)).",
    "code_edit": {
      "file": "sequence.py",
      "old": "return list(range(1, n))",
      "new": "return list(range(0, n))"
    }
  },
  {
    "id": "g14-index-correct",
    "case": "12_wrong_index",
    "fix_description": "Return the first field using index 0: return line.split(',')[0].",
    "code_edit": {
      "file": "parser.py",
      "old": "return line.split(\",\")[1]",
      "new": "return line.split(\",\")[0]"
    }
  }
]
